# The blog's HTML is generated by build_blog.py and rebuilt fresh in CI on every
# Pages deploy (see .github/workflows/pages.yml). The repo only holds the source:
# the Markdown in posts/, build_blog.py, blog.css, and this folder's README.
*.html
